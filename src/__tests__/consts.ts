export const CELL = {
    x0: 0,
    x1: 1,
    x2: 2,
    x3: 3,
    x4: 4,
    y0: 0,
    y1: 1,
    y2: 2,
    y3: 3,
    y4: 4,
    y5: 5,
};

const DEFAULT_LENGTH_ARRAY = 101;

export const STEP = Array.from(Array(DEFAULT_LENGTH_ARRAY).keys());
export const WIN_SERIES_IN_GAME = Array.from(Array(DEFAULT_LENGTH_ARRAY).keys());

// ДЛЯ ПРОВЕРКИ СТРОК, ЯЧЕЕК

export const BOARD_ROW = Array.from(Array(DEFAULT_LENGTH_ARRAY).keys());
export const BOARD_CELL = Array.from(Array(DEFAULT_LENGTH_ARRAY).keys());
export const BOARD_DISABLE_CELL = Array.from(Array(DEFAULT_LENGTH_ARRAY).keys());

